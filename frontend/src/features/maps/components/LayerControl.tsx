export interface MapLayers { crimePins:boolean; hotspots:boolean; redZones:boolean; predictive:boolean; socioEconomic:boolean }
export function LayerControl({ layers, onToggle }: { layers:MapLayers; onToggle:(key:keyof MapLayers)=>void }){return <div className="glass-card absolute right-4 top-4 z-[500] rounded-xl p-4 text-sm"><p className="mb-2 font-bold text-[var(--color-gold)]">Layers</p>{(Object.keys(layers) as Array<keyof MapLayers>).map(k=><label key={k} className="flex items-center gap-2 py-1"><input type="checkbox" checked={layers[k]} onChange={()=>onToggle(k)}/><span className="capitalize">{k.replace(/([A-Z])/g,' $1')}</span></label>)}</div>}
