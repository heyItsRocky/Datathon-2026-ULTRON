import { SearchInput, Select, Button } from '@/shared/ui-kit';
export function GraphControls({ onSearch,onLayoutChange,onFilterChange,availableTypes }: { onSearch:(q:string)=>void; onLayoutChange:(l:string)=>void; onFilterChange:(t:string[])=>void; availableTypes:string[] }){return <div className="glass-card flex flex-wrap items-center gap-3 rounded-xl p-3"><SearchInput placeholder="Search node label" onChange={e=>onSearch(e.target.value)}/><Select value="cose" onValueChange={onLayoutChange} options={['cose','grid','concentric','breadthfirst'].map(v=>({label:v,value:v}))}/><div className="flex flex-wrap gap-2">{availableTypes.map(t=><label key={t} className="text-sm"><input type="checkbox" defaultChecked onChange={e=>onFilterChange(e.target.checked?availableTypes:availableTypes.filter(x=>x!==t))}/> {t}</label>)}</div><Button variant="secondary" size="sm">Export</Button></div>}
