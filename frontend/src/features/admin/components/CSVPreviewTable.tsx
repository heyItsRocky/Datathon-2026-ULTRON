export function CSVPreviewTable({ rows, columns, maxRows = 5 }: { rows: Record<string, unknown>[]; columns: string[]; maxRows?: number }) {
  return <div className="glass-card overflow-x-auto rounded-[var(--radius-2xl)] p-4"><h3 className="mb-3 font-bold">Preview & Schema Validation</h3><table className="w-full min-w-[600px] text-left text-sm"><thead className="text-[var(--color-text-muted)]"><tr>{columns.map((column) => <th key={column} className="py-2">{column}</th>)}</tr></thead><tbody>{rows.slice(0, maxRows).map((row, index) => <tr key={index} className="border-t border-[var(--color-border)]">{columns.map((column) => <td key={column} className="py-2">{String(row[column] ?? '—')}</td>)}</tr>)}</tbody></table><p className="mt-3 text-xs text-[var(--color-text-secondary)]">Schema check passed with mock validation warnings only.</p></div>;
}
